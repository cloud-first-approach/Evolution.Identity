apiVersion: v1
kind: Service
metadata:
  name: identity-api-cluster-ip
  namespace: evolution
spec:
  type: ClusterIP
  selector:
    app: identityservice-api
  ports:
  - name: identityservice-api-port
    protocol: TCP
    port: 80
    targetPort: 80